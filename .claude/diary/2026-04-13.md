# Session Diary — 2026-04-13

## Session context
- **Date**: 2026-04-13
- **Duration**: ~3 hours
- **Focus**: Vendor pull of upstream matsim_llm_plugins + first LLM replanning runs

## What was done
- Pulled 10 new commits from matsim_llm_plugins upstream (May2025 branch, ~9700 lines added)
- Vendored all new/updated packages into src/main/java: matsimdtobjects, tools/Implement, prompts, run, plus updates to chatcommons, matsimBinding, tools, rag, chatrequest, chatresponse
- Fixed log4j → log4j2 imports in LLMReplanningStrategyModule and LLMWithinDayListener
- Resolved Maven enforcer dependency conflicts (jackson 2.21, grpc 1.77, guava, slf4j, protobuf, picocli)
- Excluded protobuf-javalite from bicycle contrib (classpath conflict with protobuf-java 4.29.3)
- Rewrote RunSiouxFallsLLMAgents to use upstream LLMIntegrationModule(ConnectionType.replanning)
- Pulled models: qwen3:14b, qwen3.5 (9.7B), qwen2.5:7b, nomic-embed-text
- Set up Qdrant vector DB via Docker
- Added error handling in LLMReplanningStrategyModule.finishReplanning() — timeout/bad tool name no longer crashes simulation
- Moved all output dirs under output/ (gitignored)
- Moved matsim_llm_plugins into project root (was sibling dir)
- Forked upstream to giulioiannelli/matsim_llm_plugins, set up origin/upstream remotes

## Decisions made
- Use BackendType.LM_STUDIO for Ollama (OLLAMA backend still unimplemented)
- Use ConnectionType.replanning (MATSim scoring has final word) as safest starting mode
- 5 AI agents, start from iteration 0
- maxTokens reduced from 10000 to 4096 to limit reasoning time

## Key findings
- ChromaDB replaced by Qdrant + langchain4j in upstream
- New DTO layer (PlanDTO/ActivityDTO/LegDTO) replaces old PlanGson for richer plan serialization
- Three new tools: ExtractPlanTool (dummy), RouterTool (real, calls TripRouter), PullAdditionalContextTool (real, RAG)
- Three execution modes: replanning (MATSim final word), controllerlistener (LLM final word), withinday (real-time LLM)

## Issues encountered
- protobuf-javalite:3.23.4 from osm4j-pbf conflicted with protobuf-java:4.29.3 → excluded from bicycle contrib
- Background bash commands with pipe buffering produced empty output files → run without pipes

## Computational runs
| Run | Model | Scenario | Iterations | Result | Notes |
|-----|-------|----------|-----------|--------|-------|
| 1 | qwen3:14b | siouxfalls | 20 (killed at it.1) | Too slow | 244s/agent, correct tool calling, massive chain-of-thought |
| 2 | gemma4:e4b | siouxfalls | 20 (crashed at it.1) | Bad tool name | 75s/agent, called `plan_tool` instead of `extract_plan` |
| 3 | qwen3.5 | siouxfalls | 20 (crashed at it.2) | Timeout on PT plan | ~24s/agent for simple plans, 8+ min for PT plans with multi-turn routing |
| 4 | qwen3.5 | siouxfalls | 10 (killed at it.1) | User had to leave | Error handling added, maxTokens reduced |

## Next steps
- Run complete 10-iteration simulation with qwen3.5
- Analyze chat logs (llm_chat_log_ChatLog_combined.jsonl) to understand slow reasoning on PT plans
- Simplify LLM task for complex plans (reduce thinking time without disabling it)
- Test qwen2.5:7b as faster alternative
- Set up Python analysis submodule for output processing

## Open questions
- Why does qwen3.5 take 8+ minutes on PT plans? Is it the multi-turn tool calling or the reasoning?
- Can we pre-process PT chains to simplify the plan before sending to LLM?
- Should we limit maxToolIterations to prevent runaway tool-calling loops?
