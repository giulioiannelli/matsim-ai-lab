# 2026-09-16 — One-command environment start with remote tunnel

## What was done

- User asked for the remote-launch command; the only recorded one was the raw
  SSH tunnel to mari (diary 2026-07-23). Made it a non-issue:
  - bashrc: added `ollamatunnel` alias (tunnel only) next to the existing
    `ollamaserv` (interactive shell on mari).
  - `scripts/matsim-env-start.sh`: new `_matsim_ensure_tunnel` step between
    Qdrant and the Ollama check. Idempotent (detects an open tunnel via pgrep),
    stops the local `ollama` service if it holds :11434, fails loudly if the
    port is held by anything else. Env-overridable: `OLLAMA_TUNNEL` (1/0),
    `OLLAMA_REMOTE_HOST`, `OLLAMA_REMOTE_SSH_PORT`, `OLLAMA_REMOTE_USER`,
    `OLLAMA_REMOTE_PORT`. Default model check updated to
    `qwen3.6:27b qwen3-embedding:0.6b` (old `qwen3.5 nomic-embed-text` would
    always warn against mari).
  - Verified: fresh source opens the tunnel and finds both models on mari;
    second source reports "already open" and leaves exactly one ssh process.

## Session recipe (now)

```bash
ai-matsim-start          # conda + qdrant + tunnel + model check
<run command> ; scripts/ollama-gpu.sh evict
```

## Next steps

- Unchanged from 2026-07-23: rerun 27B with `--max-tool-iterations` 8–10, then
  the multi-seed persona re-baseline. `migrate/contrib-llm` still unpushed.

## Research resume (afternoon)

- Offline verify: `./mvnw test -Dtest=RunMatsimTest` OK; new
  `scripts/probes/native_probe.sh` (native API: tags, chat+thinking, tool
  call, embed) all PASS on mari. Side finding: a 512-token `num_predict` is
  fully eaten by thinking on a one-sentence question (≈85% thinking).
- 27B smoke run (2 ag / 1 it / persona+cmp / cap 10 / seed 4711): 2/2
  applied, but 11.5 + 7.1 min/agent. Decomposition + probe sweep → the 27B
  spills at any ctx > 4096 on mari and is reloaded every call (see lab-log
  2026-09-16). Embedder call evicts it too.
- Decision: right-size to `qwen3.5:9b` (profile added to
  `config/model-profiles.yaml`, ctx 12288 / maxTok 6144 / cap 3072); pulled
  to mari through the tunnel. Next: probe + identical smoke, compare.
- mari facts: 2× RTX 2080 Ti 11 GB, Ollama 0.32.1, no OLLAMA_NUM_PARALLEL
  set, 125 GB RAM, one other user resident (~0.7 GB GPU).
- 9B on mari: fits at ctx 12288/16384, 65 tok/s. First 9B smoke: 1/2, 55%
  of wall = model reload (embedder evicts chat model). Added dedicated
  embedding endpoint (`embeddingHost`/`embeddingPort` + runner flags, default
  = chat server) and a local embedder Ollama on :11435 (laptop GPU), started
  by `matsim-env-start.sh` (`LOCAL_EMBEDDER=1` default). Second 9B smoke:
  2/2 applied, 233 s total vs 1119 s (27B). Residual per-round reloads traced
  to another user's llama3.1:8b jobs on mari (seen in `ollama ps`).
- `ws0-rebaseline.sh` defaults now MODEL=qwen3.5:9b + local embedder flags.
- Unit test re-run after the config change: PASS. All 3 runs in `runs.md`.
- Uncommitted: scripts/, config/model-profiles.yaml, LLMConfigGroup,
  RunSiouxFallsLLMAgents, research/persona-emergence/*, this diary,
  scripts/probes/native_probe.sh. Stray untracked
  `src/main/resources/paramReaderTrial1_newData REsult (1).csv` is not ours.

## Next steps

- Launch `MODEL=qwen3.5:9b scripts/ws0-rebaseline.sh` (≈2.5 h/seed) when
  mari is quiet; or first ask the collaborator for `OLLAMA_NUM_PARALLEL=2-3`
  and implement A2 (bounded executor in `finishReplanning`; make CSV + JSONL
  writers thread-safe) to bring a seed under 1 h.
- Optional: ask the collaborator whether shared use of mari's Ollama can be
  scheduled, since concurrent jobs add ~20 s per round to our runs.

## Correction (late afternoon): the 27B is viable — `gpuLayers`

PI's LLM specialist said mari should hold the 27B. Verified: the spill was
Ollama's memory estimate, not real pressure. Probe with `num_gpu: 999` →
17.4/17.4 GB on GPU at ctx 12288, 26.8 tok/s. Wired as profile key
`gpuLayers` (ModelProfile → LLMConfigGroup.gpuLayers →
OllamaNativeChatRequest `options.num_gpu`; 0 = server decides; client passes
it when building the native request). 27B smoke with gpuLayers + local
embedder: 2/2 applied, 397 s total (vs 1119 s), load 19 s, prompt cache OK.
Registered in runs.md; lab-log corrected; memory note corrected.
`ws0-rebaseline.sh` default MODEL back to qwen3.6:27b. `ollama-gpu.sh evict`
now only unloads our own models (OLLAMA_OUR_MODELS); `evict-all` = old
behaviour — mari is shared and another user's llama3.1:8b was seen resident.
Mid-size alternatives that exist in the registry, should the 27B ever be
needed elsewhere: gpt-oss:20b (MoE, reasoning-effort control), qwen3:14b,
qwen3:30b-a3b (MoE), mistral-small3.2:24b, magistral:24b, phi4:14b.

## 3×3 experiment (seed 4721) + two findings

- Ran `SEEDS=4721 AGENTS=3 ITERS=3 scripts/ws0-rebaseline.sh` as a user
  systemd unit (`systemd-run --user`; plain background/setsid launches die
  with the tool's 10-min cap). 9/9 applied, protocol + persona all green.
- **Bug fixed**: `ActivityDTO.toBaseClass` dropped `maximumDuration` on stage
  activities → pt agents froze at the stop all day and *scored better*.
  Now `maximumDuration 0` for `* interaction` types without end time; test
  `ActivityDTOStageActivityTest`.
- **Ground gridlocked**: full-scale Sioux Falls, 75.8k/84.1k stuck at it.0,
  71k at it.10 (old run). All experiments to date ran on it. Decision pending
  (warm-up vs population sample) — see lab-log.
- Evaluation guide written: `research/persona-emergence/evaluation-checkpoints.md`
  (+ artifact https://claude.ai/artifact/TDyexCi43QUpoYQj4GprvZ); checkpoint 0
  "ground health" added after the finding.

## Night: panel plan accepted → WP1 + WP2

- Proved to the user that the 27B activity on mari was another tenant's:
  with our tunnel down for >1 min, mari loaded a fresh 27B runner with
  num_ctx 4096 (ours is 12288). `ollama` is the daemon's service account, not
  our login (`ollama-server`).
- PI: "go with the defaults" → plan status ACCEPTED (panel 200, bottom-20 %
  trigger, 10 % sample, transfer deferred).
- WP1: `org.matsim.project.ground` (GroundOptions mixin, InnovationBoost via
  StrategyManager change requests), `RunSiouxFalls` → picocli. 8 unit tests.
  Warm-up `siouxfalls-s0.10-b10-seed4711` registered in runs.md (healthy,
  7.5 min).
- WP2: `matsimBinding.panel` (PanelSelection + test, PanelStrategyChooser,
  PanelExperienceTracker, PanelModule); LLMConfigGroup `panelMode`,
  `maxQueriesPerIteration`, `triggerScoreDropQuantile`; runner `--panel`,
  `--max-queries`, `--trigger-quantile`; `tagAIAgents` (no subpop move);
  legacy path moved to `configureLegacySubpopulations`. LLM strategy bound as
  Singleton (first smoke sent 0 queries because of duplicate instances).
- Runs launched as `systemd-run --user` units with `scripts/ollama-gpu.sh
  evict` chained after the java process (always evict on exit).

### Next
- Read the panel smoke result (`output/siouxfalls-c0.10-warm-…-panel20q3`):
  queries applied, `llm_panel_selection.csv` reasons, per-query wall time.
- Commit WP1+WP2; then WP3 (one-shot precomputed context) and WP4
  (`matsim-analyze ground-health`, per-agent decision table).

