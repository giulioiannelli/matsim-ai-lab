# 2026-09-18 — WP3 speed programme: 374 s → 53 s per agent

## What was done
- Launched the 50-agent / budget-5 panel run on the warmed 10 % ground
  (tool path). Stopped at iteration 4 on PI request (laptop had to close);
  15/15 applied, 181 s median per agent once the model stayed resident.
  Registered as partial.
- Diagnosed the reload problem: keep-alive works (`scripts/probes/
  keepalive_probe.sh`), reloads come from another tenant alternating models
  with us; mari serves one request at a time (no concurrency gain).
- Built and verified on the laptop 9B (`--llm-port=11435`), then on the 27B:
  one-shot context (`matsimBinding.oneshot`), decision output
  (`tools.Implement.DecideTripsTool`, `--decision-output`), trimmed tool
  set, `--thinking` / `--reasoning-style` levers, `scripts/speed-sweep.sh`,
  `matsim-analyze timing` (matsim-py-analysis, own repo, committed).
- 27B results (same 12/9-agent smoke, seed 4721): baseline 374 s →
  decision 66 s → brief 53 s → no-thinking 46 s median per agent, all
  plans applied. Campaign default: `--decision-output --reasoning-style=brief`.
- Bugs fixed with tests: available_modes car location (end-of-day instead
  of at-place), stringified decision lists dropped silently, linkless route
  stub hanging MATSim's plans writer.
- Commits: fcc4e2b, 167d64d, 041562b, b60e150, d370862, e6bb211
  (+ matsim-py-analysis e68ef57).

## Next
- WP4: `ground-health` + per-agent decision table in matsim-analyze;
  persona metrics on the brief-variant transcripts (does brief keep the
  first-person grounding?).
- Plan step 8: run the MATSim side on mari so runs survive the laptop.
- First campaign run: panel 200, budget 10, 25 iterations, brief, ≈ 2.5 h.
