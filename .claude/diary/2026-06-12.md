# Session Diary — 2026-06-12

## Session context
- **Date**: 2026-06-12
- **Focus**: Recover project status after lost chat history; commit untracked
  work; move planning local-first.

## What was done

Chats since ~Apr 21 were lost. Reconstructed status from `.claude/diary/`,
the memory store, `~/.claude/plans/`, and git history of both repos.

- **Status recovered.** The matsim project has been **dormant since 2026-04-21**
  (last commit, last diary, last file mtime all Apr 21). The May/Jun plans in
  the global plans dir belong to *other* projects (multifun-brain, lrgsglib),
  not matsim — so nothing matsim was lost to the missing chats; it was paused
  mid-roadmap at the Phase-4 close-out.
- **Critical git finding fixed (in progress this session).** HEAD tracked only
  31 of 109 Java files; ~78 source files + all docs/research/scripts/.claude
  were untracked on disk with no git safety net. Committing them now.
- **Reference test kept.** `RunMatsimTest.java` showed as a working-tree
  deletion; restored it (`git restore`) per user.
- **Planning moved local-first.** Rebuilt the lost master plan as
  `.claude/plans/persona-replanning-roadmap.md` (meaningful name, committed,
  in-repo) so it can't be washed out by global memory again. Added a
  "Planning & continuity (local-first)" note to `CLAUDE.md`.
- **gitignore hardened.** Added `/scenarios/*/output/` (2.4M of regenerable
  MATSim run output was untracked and would otherwise be committed).

## Decisions made

- **Commit only to giulioiannelli repos.** `matsim-ai-lab` origin is
  `github.com/giulioiannelli/matsim-ai-lab`; the plugin repo is gitignored, so
  no commit here can reach auzpatwary37. Confirmed before committing.
- **Excluded** `src/main/resources/paramReaderTrial1_newData REsult (1).csv` —
  stray cruft file (messy name, sole file in resources). Left untracked, flagged
  to user.
- **Resume point = Behavior Realization loop**, not new infra. The three project
  goals (persona emergence, correct tool use, meaningful routing strategy) are
  not yet demonstrated with the small model; persona-memory and the model
  bake-off are deferred until base behavior works (per
  `feedback_never_regress_baseline`).

## Next steps
- Run one gated multi-seed validation (persona + `--enable-comparison-tools`)
  to get a current metric baseline for the three goals, then iterate prompts.
- Decide whether the comparison addendum regresses the legacy prompt variant.
- Optional: push the new commits to the fork when ready (not done this session).

## Notes
- Plugin repo (`matsim_llm_plugins/`) is clean, branch `May2025`, last commit
  Apr 13. Sync against the fork (`origin`), not upstream, when resuming.
