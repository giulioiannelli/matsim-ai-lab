# 2026-09-23 — Campaign run 1 launched, stopped, fixed, relaunched

## What was done
- Pushed both repos, added the per-iteration ETA reporter
  (`org.matsim.project.progress.EtaReporter`, `progress.txt`, commit 13a31dd)
  and launched campaign run 1 at 15:37 (panel 200, budget 10, 25 iterations,
  brief decision output, seed 4721) as unit `matsim-campaign-1`.
- Read the first two iterations while they ran: 19/20 agents kept their day,
  the one change left the car at the destination. Root cause in the one-shot
  block (per-place modes under the current plan forbade the pt→car switch
  for every car owner). PI: "ok stop fix and relaunch".
- Fix: `AvailableModesTool.vehicleAccess` (owned + start location),
  `OneShotContextBuilder` rewritten around a "Your vehicles" block and
  chain-conditional car/bike route options (`vehicleRequirements`),
  `DecideTripsTool.verifyArguments` follows the vehicle along the decided
  chain, prompt ground rule and one-shot guidance reworded, `--max-tokens`
  hard cap (3072 for brief). 94 Java tests pass.
- Smoke `matsim-smoke-chain` (1 replanning iteration, same seed and panel, so
  the same 10 agents as the campaign's iteration 1) launched 16:17.
- Server finding: a colleague runs the same 27B at ctx 4096 between our
  calls; every round reloads (33 % of LLM time). Not fixable from our side.
- Docs: runs.md row, lab-log entry, guide 04 (speed flags + vehicle block),
  plan status.

- Smoke read 16:30: 10/10 applied, 1 round each, 3 pt→car switches (out and
  back), 65 s/agent of which 33 s reload. Campaign run 2 launched with
  `--max-tokens=3072`.

## Next
- Read the smoke: do the car owners on pt switch now, is the chain accepted
  by `decide_trips`, any decision rejected? Then relaunch the campaign
  (`--max-tokens=3072` in EXTRA) and register it.
- Plan step 8 (MATSim side on mari) is now the main speed lever left.
