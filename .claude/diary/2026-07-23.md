# 2026-07-23 — Remote Ollama on mari: tunnel, probes, first 27B run

## What was done

- **Moved LLM inference to a remote GPU server** (`mari.dinfo.unifi.it`, SSH port 25746, user `ollama-server`) owned by a collaborator. Zero code changes: an SSH local port forward makes the remote Ollama appear at `localhost:11434`, which is what all runners, the pre-flight hook, and the analysis suite already target.
  ```
  ssh -N -f -p 25746 -o ExitOnForwardFailure=yes -o ServerAliveInterval=30 \
      -L 11434:localhost:11434 ollama-server@mari.dinfo.unifi.it
  ```
  Local Ollama must be stopped first (port clash). Qdrant stays local.
- **Offline verification (per verify-first rule)**: 3 probes through the tunnel — models list, plain chat, structured tool call — all passed on `qwen3.5:0.8b`, then repeated on `qwen3.6:27b`.
- **Remote models installed**: `qwen3.6:27b` (collaborator), `nomic-embed-text` (pulled remotely via `/api/pull` through the tunnel — required for the Qdrant experience-embedding path), `qwen3.5:0.8b` (kept for wire tests).
- **Model profiles added** (`config/model-profiles.yaml`): `qwen3.5:0.8b` and `qwen3.6:27b`, both `ollama_native` + thinking.
- **27B envelope probe-validated**: ctx 12288 / maxTokens 6144 → CPU spill stays at 1.0 GB (same as ctx 4096), 21.6 tok/s generation. mari's two GPUs give ~18–19 GB combined usable VRAM; the Q4 27B never fully fits, ~1 GB always on CPU.
- **GPU hygiene** (collaborator's machine is shared; requirement: nothing resident after runs): new `scripts/ollama-gpu.sh [status|evict]`; every simulation launch now chains `; scripts/ollama-gpu.sh evict`. Verified clean after both runs.

## Computational runs

| Run | Model | Agents | Result |
|-----|-------|--------|--------|
| `siouxfalls-qwen3.5-0.8b-T0.3-N4096-reasoning-persona` | 0.8b (wire test) | 2 | Plumbing perfect (0 protocol failures); both agents failed to produce plans (expected — model too small). 51s total LLM time. |
| `siouxfalls-qwen3.6-27b-T0.3-N6144-reasoning-persona` | 27B | 2 | **Agent 20997_1: SUCCESS, plan applied** (2 rounds, 87s). Agent 15840_2: 5 clean rounds, hit the test's `--max-tool-iterations=4` cap (247s). 0 parsing/verification/execution failures across 7 tool calls. |

Same seed ⇒ same agents in both runs ⇒ direct 0.8b↔27B comparison.

## Decisions

- Tunnel over direct connection (no `OLLAMA_HOST=0.0.0.0`, no firewall changes, hook keeps working).
- Fork remote of `matsim_llm_plugins` switched from SSH to HTTPS + gh credential helper (GitHub rejects this machine's SSH key). Also confirmed: no new upstream commits; lab branch already contains everything.
- 27B profile: ctx 12288 / maxTokens 6144 (probe-validated; laptop envelope 8192/4096 was a 16 GB-card constraint, not a model constraint).

## Afternoon session — campaign planning

Interviewed the PI on the phase's open decisions (scenario, finish line, speed
levers, memory scope, concert role, publication, affect depth, mari budget) and
mapped the full tooling inventory with an exploration pass. Created the
**persona-emergence campaign** at `research/persona-emergence/` (plan of
record: README.md; lab-log.md; runs.md registry). Updated: research/README.md
index, master roadmap pointer, CLAUDE.md stale proposed-tools table.

Inventory highlights that shaped the plan:
- Replanning loop is strictly **sequential** (blocking HTTP per agent) — #1
  speed blocker → WS-A2.
- `persona_memory` / `update_persona_memory` are the only unimplemented
  proposed tools → WS-A3 (also the persona-persistence mechanism).
- Sentiment/decision NLP already exists in matsim-py-analysis (`sentiment`,
  `classify`, `decisions`, `feedback`) — extend, don't rebuild → WS-C1.
- **Landmine**: Qdrant experience docs persist across runs (cleanup only wired
  in controllerlistener mode; fixed collection name) → contamination fix is
  WS-0's first task, blocker for multi-seed claims.
- Within-day path is a prototype (fires at mobsim init only, legacy prompt, no
  validators/stats) — consistent with "concert = north star only" decision.

## Evening session — WS-0 executed, rebaseline batch running, first A1 result

- Machine shutdown killed the tunnel; recovery was trivial (all work is
  file-based). SSH key now installed on mari (`ssh-copy-id` from a real
  terminal) — tunnels are passwordless from here on; the in-session `!`
  prompt can't ask for passwords (no ssh-askpass), so key auth is required.
- Server change: collaborator pulled `qwen3.6:latest` = **36B, 23.9 GB** —
  larger than mari's combined VRAM, would spill heavily; advised against it.
  `qwen3.5:0.8b` was removed. Workhorse remains `qwen3.6:27b`.
- WS-0 fixes implemented, verified, and committed as **03e5058** (also carries
  the campaign plan + scripts). Embedder default now `qwen3-embedding:0.6b`
  (1024-d; per-run collections absorb the dimension change).
- **Rebaseline batch launched** via `scripts/ws0-rebaseline.sh` (3 seeds ×
  5 agents × 10 it, persona+cmp, cap 10, 27B; early-stop <60%; evict per
  seed). Runs at commit 03e5058.
- **A1 timing decomposition** (on the morning 27B run): generation = **91%**
  of wall time; prompt eval 7% (KV prefix caching works — 4× cheaper after
  round 1); model load 2%; SSH tunnel + HTTP overhead **0.04%** (remote is
  free). **~68% of generated tokens are reasoning.** Conclusion: the speed
  levers are thinking-token reduction and agent parallelism; endpoint and
  prompt-eval optimizations are not worth effort. `thinkingTokenCap` is
  advisory-only today — enforcement experiment queued in A1.

## Next steps

- Rerun 27B with a sane `--max-tool-iterations` (8–10): agent 15840_2 was still productive when capped at 4.
- Then the deferred multi-seed persona re-baseline (diary 2026-06-12), now on the 27B.
- Optional: install SSH key on mari (`ssh-copy-id -p 25746 ollama-server@...`) so tunnels don't prompt for password; ask collaborator for `nvidia-smi` output to evaluate pinning Ollama to one GPU (`CUDA_VISIBLE_DEVICES`).
- Unpushed work reminder: `migrate/contrib-llm` (this repo) and plugin `lab` branch exist only locally.
